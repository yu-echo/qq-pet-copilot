.venv/
__pycache__/
*.pyc
.env
screenshots
# 打包产物（spec 是 build.py 的输入，需入库，不再忽略）
build/
dist/
# 运行状态（进度、日志、调试图）
runs/
# 控件树抓页产物（tools/dump_hierarchy.py）
xml/
# 个人配置（模板为 config.example.yaml）
config.yaml

# 第三方二进制/离线包（scrcpy、frida-server 等），build 时下载或本地放入，不入库
resources/*
!resources/moneybag-friend.png
!resources/svip-entry.png
profiles/
profiles.json
.pyinstaller-cache/
_release_stage/
release_notes.md
*.zip
